# ChessPuzzleBot — راهنمای راه‌اندازی

ربات با **کوتای پلکانی هفتگی و دائمی** آپلود می‌کند (از `launch_date` شمرده می‌شود):
- هفتهٔ ۱: روزی **۲ ویدیو** — هفتهٔ ۲: **۳** — هفتهٔ ۳: **۵** — هفتهٔ ۴ و بعد: **۸ برای همیشه**
- اسلات‌های آپلود خودکار بین 08:00 تا 22:00 پخش می‌شوند
- هر ویدیو = **۵ پازل × ۲ دقیقه = ۱۰ دقیقه**
برای هر ویدیو:
- **تامنیل جذاب شطرنجی** ساخته و روی ویدیو ست می‌شود
- **۲ شورت** از همان پازل‌ها ساخته و **به ویدیو لینک** می‌شوند
  (لینک ویدیو در توضیحات شورت هست)
- **تایتل، دیسکریپشن، کپشن و هشتگ‌ها را خودِ ربات** تولید می‌کند
- شماره پازل‌ها خودکار زیاد می‌شود (Puzzle #1, #2, ...)
- جواب هیچ‌وقت روی بورد نمایش داده نمی‌شود — فقط «Comment the answer!»

قالب ویدیو (همان نمونهٔ تأییدشده): بورد کامل سمت چپ، متن‌ها سمت راست
(Puzzle #N → White/Black to move → Comment the answer!)، مهره‌های رسمی
cburnett (lichess) با تم قهوه‌ای. شورت: بورد تمام‌عرض وسط با متن بالا/پایین.

موسیقی: قطعهٔ آرام ۴ لایه (بیس + آرپژ هارپ + پد ویلن + ملودی پیانو) با
پیشروی آکورد متنوع — هر ویدیو کلید و ملودی متفاوتی دارد.

## گام ۱ — نصب یک بار (اینترنت لازم دارد)
CMD را باز کنید:
```
cd "مسیر پوشه chessyt"
pip install -r requirements.txt
```

## گام ۲ — دانلود ۱۰۰,۰۰۰ پازل معتبر
```
python download_puzzles.py
```
دیتابیس رسمی و رایگان Lichess دانلود می‌شود، پازل‌های ریتینگ ۱۴۰۰ تا ۲۵۰۰
استخراج و **تک‌تک اعتبارسنجی** می‌شوند (FEN و حرکات راه‌حل با python-chess
چک می‌شود تا هیچ پازل خرابی وارد استخر نشود).

## گام ۳ — اتصال به یوتیوب (بدون پسورد!)
⚠️ رمز جیمیل را هیچ‌وقت داخل فایل نگذارید.
1. برو به https://console.cloud.google.com → پروژه جدید بساز
2. «APIs & Services → Library» → **YouTube Data API v3** → Enable
3. «OAuth consent screen» → External → بساز (تست کافی است)
4. «Credentials → Create Credentials → OAuth client ID» → **Desktop app**
5. فایل JSON را دانلود و **داخل همین پوشه** با نام `client_secrets.json` بگذار
6. اولین اجرای ربات، مرورگر را باز می‌کند → با اکانت یوتیوبت لاگین کن.
   بعد از آن، توکن ذخیره می‌شود (`token.pickle`) و دیگر لازم نیست.

## گام ۴ — اجرا
روی `start.bat` دابل‌کلیک کن. پنجره CMD باز می‌شود:
- ساختن ویدیو، تامنیل و شورت‌های لینک‌شده خودکار است
- اگر اینترنت قطع باشد، هر ۶۰ ثانیه چک می‌کند و بعد از وصل‌شدن آپلود می‌کند
- برای آپلود فوری همین الان: `DAZAI.bat`
- بعد از هر آپلود کامل می‌نویسد: `BATCH DONE ✅`
- **توقف:** پنجره را ببند یا Ctrl+C

## تنظیمات
`config.json`: تاریخ شروع کانال (`launch_date`)، جدول پلکانی سهمیهٔ هفتگی
(`weekly_videos_per_day`: "1":2, "2":3, "3":5, "4":8, "default":8 — دائمی)،
بازهٔ اسلات‌ها (`first_upload_time`/`last_upload_time`)، تعداد پازل هر ویدیو
(`puzzles_per_video`)، مدت هر پازل (`seconds_per_puzzle`)، تعداد شورت هر ویدیو
(`shorts_per_video`)، مدت شورت (`short_seconds`)، بازه ریتینگ.

## سند کامل برای هوش مصنوعی
`AI_SPEC.md` همهٔ جزئیات (قالب دقیق، رنگ‌ها، فونت‌ها، موسیقی، زمان‌بندی،
قواعد) را مشخص می‌کند — هر هوش مصنوعی دیگری با همین فایل می‌تواند
این ربات را دقیقاً همین‌گونه بازتولید کند.

`state.json` پیشرفت را نگه می‌دارد — پازل‌ها به ترتیب و بدون تکرار استفاده می‌شوند.
